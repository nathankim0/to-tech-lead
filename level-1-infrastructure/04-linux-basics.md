# Linux 기본

## 1. 한 줄 요약

**Linux는 대부분의 서버에서 사용되는 오픈소스 운영체제로, 모바일 개발자가 백엔드와 협업하려면 기본 명령어를 알아야 합니다.**

## 2. 쉽게 설명

### 모바일 개발자 관점에서

여러분이 만든 모바일 앱이 통신하는 서버의 90% 이상이 Linux에서 동작합니다. 서버 로그 확인, 배포 상태 점검, 문제 디버깅을 위해 Linux 명령어는 필수입니다.

**스마트폰에 비유하면:**
- Linux는 서버의 운영체제 (iOS, Android와 같은 역할)
- 터미널은 서버와 대화하는 창구 (Settings 앱과 비슷)
- 명령어는 서버에게 내리는 지시 (터치 제스처와 비슷)

**모바일 개발자가 Linux를 접하는 실제 상황:**
- "API가 500 에러를 반환해요" → 서버 로그 확인 필요
- "배포 후 앱이 이상하게 동작해요" → 새 버전이 제대로 배포되었는지 확인
- "CI/CD 빌드가 실패했어요" → Jenkins/GitHub Actions 로그 분석
- "로컬에서 서버 환경 테스트하고 싶어요" → Docker로 Linux 환경 구축

### 왜 모바일 개발자가 Linux를 알아야 하나요?

1. **서버 로그 확인**: API 오류의 원인을 서버 로그에서 찾아야 할 때
2. **배포 상태 점검**: 새 버전이 제대로 배포되었는지 확인할 때
3. **문제 디버깅**: "서버 응답이 느려요"라고 할 때 원인 파악
4. **CI/CD 이해**: GitHub Actions, Jenkins 등 빌드 스크립트 이해
5. **Docker 사용**: 로컬에서 서버 환경 구축 시

### 핵심 개념

| 개념 | 설명 | 비유 |
|------|------|------|
| 파일 시스템 | 모든 것이 파일로 관리됨 | iOS의 파일 앱 |
| 프로세스 | 실행 중인 프로그램 | 실행 중인 앱 |
| 권한 | 읽기/쓰기/실행 권한 | 앱 권한 설정 |
| 패키지 관리 | 프로그램 설치/삭제 | App Store |
| 서비스 | 백그라운드 프로그램 | Background App Refresh |
| 쉘 | 명령어 해석기 | Siri/Google Assistant |
| 환경변수 | 시스템 설정 값 | Build Configuration |

## 3. 구조 다이어그램

### Linux 파일 시스템 구조 (상세)

```
/                          ← 루트 디렉토리 (최상위)
├── bin/                   ← 기본 명령어 (ls, cp, mv, cat, grep)
│                             모든 사용자가 사용 가능
├── sbin/                  ← 시스템 관리 명령어 (reboot, fdisk)
│                             root만 사용 가능
├── etc/                   ← 설정 파일들 ★ 중요
│   ├── nginx/             ← Nginx 설정
│   │   ├── nginx.conf     ← 메인 설정
│   │   └── sites-enabled/ ← 가상 호스트 설정
│   ├── ssh/               ← SSH 설정
│   │   └── sshd_config    ← SSH 서버 설정
│   ├── hosts              ← 로컬 DNS (호스트 파일)
│   ├── passwd             ← 사용자 정보
│   ├── environment        ← 환경변수
│   └── systemd/           ← 서비스 설정
├── home/                  ← 사용자 홈 디렉토리 ★ 중요
│   └── ubuntu/            ← ubuntu 사용자의 홈 (~)
│       ├── .bashrc        ← 쉘 설정 (별칭, 환경변수)
│       ├── .ssh/          ← SSH 키
│       │   ├── id_rsa     ← 개인키 (절대 공유 X)
│       │   └── id_rsa.pub ← 공개키
│       └── projects/      ← 프로젝트 폴더
├── var/                   ← 변동 데이터 ★ 매우 중요
│   ├── log/               ← 로그 파일들
│   │   ├── nginx/         ← Nginx 로그
│   │   │   ├── access.log ← 접속 로그
│   │   │   └── error.log  ← 에러 로그
│   │   ├── syslog         ← 시스템 로그
│   │   ├── auth.log       ← 인증 로그 (로그인 시도)
│   │   └── app/           ← 애플리케이션 로그
│   │       └── api.log    ← API 서버 로그
│   └── www/               ← 웹 서버 파일
│       └── html/          ← 정적 파일 (HTML, CSS, JS)
├── usr/                   ← 사용자 프로그램
│   ├── bin/               ← 사용자 명령어 (python, node)
│   ├── lib/               ← 라이브러리
│   └── local/             ← 로컬 설치 프로그램
│       └── bin/           ← 직접 설치한 프로그램
├── opt/                   ← 추가 애플리케이션 (써드파티)
│   └── google/chrome/     ← 예: Chrome 브라우저
├── tmp/                   ← 임시 파일 (재부팅 시 삭제)
├── proc/                  ← 프로세스 정보 (가상 파일시스템)
│   ├── cpuinfo            ← CPU 정보
│   └── meminfo            ← 메모리 정보
└── dev/                   ← 디바이스 파일
    ├── null               ← 블랙홀 (버릴 출력용)
    └── sda                ← 디스크 장치

💡 모바일 개발자가 자주 접근하는 경로:
   /var/log/          → 서버 로그 확인
   /etc/nginx/        → Nginx 설정 확인
   /home/ubuntu/      → 소스 코드, 배포 스크립트
```

### 파일 권한 구조 (상세)

```
-rwxr-xr-x  1  ubuntu  ubuntu  4096  Jan 15 10:30  script.sh
│└┬┘└┬┘└┬┘  │  └──┬──┘ └──┬──┘ └─┬─┘ └─────┬─────┘ └───┬───┘
│ │  │  │   │     │       │      │         │           │
│ │  │  │   │     │       │      │         │           └─ 파일명
│ │  │  │   │     │       │      │         └─ 수정 날짜/시간
│ │  │  │   │     │       │      └─ 파일 크기 (bytes)
│ │  │  │   │     │       └─ 소유 그룹
│ │  │  │   │     └─ 소유자
│ │  │  │   └─ 하드링크 수
│ │  │  └─ Others (기타 사용자) 권한: r-x
│ │  └─ Group (그룹) 권한: r-x
│ └─ Owner (소유자) 권한: rwx
└─ 파일 타입: - (파일), d (디렉토리), l (심볼릭 링크)

권한 문자와 숫자 변환:
┌─────────────────────────────────────────────────────────┐
│  r (read)    = 4   →  파일: 읽기, 디렉토리: 목록 보기   │
│  w (write)   = 2   →  파일: 수정, 디렉토리: 파일 생성   │
│  x (execute) = 1   →  파일: 실행, 디렉토리: 진입 (cd)   │
│  - (없음)    = 0                                        │
└─────────────────────────────────────────────────────────┘

예시:
  rwxr-xr-x = 755 = 소유자(7) + 그룹(5) + 기타(5)
                  = (4+2+1) + (4+0+1) + (4+0+1)

  rw-r--r-- = 644 = 소유자(6) + 그룹(4) + 기타(4)
                  = (4+2+0) + (4+0+0) + (4+0+0)

  rwx------ = 700 = 소유자만 모든 권한

💡 모바일 개발자가 자주 쓰는 권한:
   chmod 755 script.sh   → 실행 스크립트 (모두 실행 가능)
   chmod 600 id_rsa      → SSH 개인키 (소유자만)
   chmod 644 config.json → 설정 파일 (모두 읽기 가능)
```

### 프로세스 라이프사이클

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Linux 프로세스 상태 다이어그램                     │
└─────────────────────────────────────────────────────────────────────┘

                      프로그램 실행
                           │
                    ┌──────▼──────┐
                    │   Created   │  fork() 또는 exec()로 생성
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │    Ready    │  CPU 할당 대기 (실행 가능 상태)
                    └──────┬──────┘
                           │
          CPU 시간 다 됨   │   CPU 할당받음
          (타임 슬라이스)  │
                    ┌──────▼──────┐
          ┌─────────│   Running   │←──────────┐
          │         └──────┬──────┘           │
          │                │                  │ I/O 완료
          │                │ I/O 요청         │ 또는 이벤트
          │         ┌──────▼──────┐           │
          │         │   Waiting   │───────────┘
          │         │  (Blocked)  │  디스크, 네트워크 등 대기
          │         └─────────────┘
          │
          │         ┌─────────────┐
          │         │   Stopped   │  SIGSTOP 시그널 (Ctrl+Z)
          │         │   (정지)    │  SIGCONT로 재개
          │         └─────────────┘
          │
          │                │ exit() 또는 시그널
          │         ┌──────▼──────┐
          └────────>│  Terminated │  좀비 상태 또는 완전 종료
                    │   (Zombie)  │  부모 프로세스가 wait() 호출
                    └─────────────┘

💡 ps 명령어에서 보이는 상태:
   R (Running)   - 실행 중 또는 실행 가능
   S (Sleeping)  - 대기 중 (인터럽트 가능)
   D (Disk)      - 디스크 I/O 대기 (인터럽트 불가)
   Z (Zombie)    - 종료됐지만 정리 안 됨
   T (Stopped)   - 정지됨 (Ctrl+Z)
```

### 표준 입출력 리다이렉션

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Linux 표준 입출력 (I/O)                           │
└─────────────────────────────────────────────────────────────────────┘

모든 프로세스는 3개의 표준 스트림을 가짐:

┌───────────────┐
│               │     0: stdin  (표준 입력)  ← 키보드
│   Process     │     1: stdout (표준 출력)  → 화면 (정상 출력)
│               │     2: stderr (표준 에러)  → 화면 (에러 메시지)
└───────────────┘

리다이렉션 예시:

# 출력을 파일로 저장
command > output.txt        # stdout → 파일 (덮어쓰기)
command >> output.txt       # stdout → 파일 (추가)

# 에러도 함께 저장
command > output.txt 2>&1   # stdout + stderr → 파일
command 2> error.txt        # stderr만 → 파일
command &> all.txt          # 모든 출력 → 파일

# 출력 버리기
command > /dev/null         # stdout 버리기
command 2> /dev/null        # stderr 버리기
command &> /dev/null        # 모든 출력 버리기

# 파이프라인 (명령어 연결)
cat log.txt | grep "ERROR" | wc -l
     ↓           ↓           ↓
  파일 읽기   필터링      줄 수 세기

💡 실제 사용 예시:
   # 로그에서 에러만 추출하여 파일로 저장
   grep "ERROR" /var/log/app.log > errors.txt

   # 에러가 나도 무시하고 실행
   rm -rf temp/ 2>/dev/null

   # 백그라운드 실행 + 로그 저장
   nohup ./server.sh > server.log 2>&1 &
```

## 4. 실무 적용 예시

### 예시 1: 필수 Linux 명령어 모음 (확장판)

```bash
# ==================== 파일/디렉토리 탐색 ====================

# 현재 디렉토리 확인
pwd
# /home/ubuntu/projects

# 파일 목록 보기
ls              # 기본 목록
ls -la          # 숨김 파일 포함 + 상세 정보 (가장 많이 사용!)
ls -lh          # 파일 크기를 읽기 쉽게 (1K, 2M, 3G)
ls -lt          # 수정 시간순 정렬 (최신 먼저)
ls -ltr         # 수정 시간순 역순 (오래된 것 먼저)
ls -la *.log    # .log 파일만

# 디렉토리 이동
cd /var/log     # 절대 경로로 이동
cd ..           # 상위 디렉토리
cd ~            # 홈 디렉토리 (~는 /home/사용자명)
cd -            # 이전 디렉토리로 돌아가기
cd              # cd만 치면 홈으로

# 디렉토리 생성/삭제
mkdir project             # 디렉토리 생성
mkdir -p a/b/c/d          # 중간 디렉토리도 함께 생성 (자주 사용!)
rmdir empty_dir           # 빈 디렉토리 삭제
rm -rf old_project        # ⚠️ 디렉토리와 내용물 모두 삭제 (확인 없음!)

# 파일 정보
file script.sh            # 파일 타입 확인
stat file.txt             # 상세 정보 (크기, 권한, 시간 등)


# ==================== 파일 조작 ====================

# 파일 복사
cp file.txt backup.txt             # 파일 복사
cp file.txt /backup/               # 다른 디렉토리로 복사
cp -r dir1 dir2                    # 디렉토리 복사 (-r: recursive)
cp -p file.txt backup.txt          # 권한, 시간 유지

# 파일 이동/이름 변경
mv old.txt new.txt                 # 이름 변경
mv file.txt /backup/               # 파일 이동
mv *.log /var/log/archive/         # 여러 파일 이동

# 파일 삭제
rm file.txt                        # 파일 삭제
rm -i file.txt                     # 삭제 전 확인
rm -f file.txt                     # 강제 삭제 (확인 없음)
rm -rf directory/                  # ⚠️ 디렉토리 전체 삭제

# 파일 내용 보기
cat config.json              # 전체 내용 출력 (짧은 파일용)
cat -n config.json           # 줄 번호와 함께
head -20 log.txt             # 처음 20줄
tail -50 log.txt             # 마지막 50줄
tail -f log.txt              # 실시간 로그 확인 ★ 매우 중요!
tail -f -n 100 log.txt       # 최근 100줄 + 실시간
less large_file.txt          # 페이지 단위로 보기
                             # (q: 종료, /: 검색, n: 다음, N: 이전)


# ==================== 텍스트 검색/처리 ====================

# grep: 텍스트 검색 ★ 매우 자주 사용
grep "ERROR" app.log                    # ERROR 포함된 줄
grep -i "error" app.log                 # 대소문자 무시 (-i: ignore case)
grep -n "Exception" app.log             # 줄 번호 표시
grep -c "ERROR" app.log                 # 매칭된 줄 수만 출력
grep -v "DEBUG" app.log                 # DEBUG 제외 (-v: invert)
grep -r "TODO" ./src/                   # 디렉토리 내 재귀 검색
grep -A 5 "ERROR" app.log               # ERROR 이후 5줄도 표시 (After)
grep -B 3 "ERROR" app.log               # ERROR 이전 3줄도 표시 (Before)
grep -C 3 "ERROR" app.log               # ERROR 전후 3줄 표시 (Context)
grep "2024-01-15.*ERROR" app.log        # 정규표현식 사용

# 자주 쓰는 grep 조합
grep "ERROR\|WARN" app.log              # ERROR 또는 WARN
grep -E "ERROR|WARN" app.log            # 확장 정규식
grep "userId=123" access.log            # 특정 사용자 요청
grep "POST /api/users" access.log       # 특정 API 요청

# find: 파일 찾기
find . -name "*.log"                    # 현재 디렉토리에서 .log 파일 찾기
find /var/log -name "*.log" -mtime -1   # 최근 1일 내 수정된 로그
find . -type f -size +100M              # 100MB 이상 파일 찾기
find . -name "*.tmp" -delete            # .tmp 파일 찾아서 삭제
find . -name "node_modules" -type d     # node_modules 디렉토리 찾기

# awk: 텍스트 처리 (컬럼 기반)
awk '{print $1}' access.log             # 첫 번째 컬럼 (IP 주소)
awk '{print $9}' access.log             # 9번째 컬럼 (상태 코드)
awk '/ERROR/ {print}' app.log           # ERROR 포함된 줄

# sed: 텍스트 치환
sed 's/old/new/g' file.txt              # old를 new로 전부 치환
sed -i 's/old/new/g' file.txt           # 파일 직접 수정 (-i: in-place)

# sort, uniq: 정렬 및 중복 제거
sort file.txt                           # 정렬
sort -r file.txt                        # 역순 정렬
sort -n file.txt                        # 숫자로 정렬
sort file.txt | uniq                    # 중복 제거
sort file.txt | uniq -c                 # 중복 횟수 표시

# wc: 단어/줄 수 세기
wc -l file.txt                          # 줄 수
wc -w file.txt                          # 단어 수
wc -c file.txt                          # 바이트 수


# ==================== 프로세스 관리 ====================

# 프로세스 확인
ps aux                       # 모든 프로세스 보기
ps aux | grep java           # Java 프로세스 찾기
ps aux | grep -v grep        # grep 자체 제외
pgrep -f "node app.js"       # 이름으로 PID 찾기
top                          # 실시간 프로세스 모니터 (q로 종료)
htop                         # 더 보기 좋은 모니터 (설치 필요)

# 프로세스 종료
kill 1234                    # PID 1234 프로세스에 SIGTERM (정상 종료 요청)
kill -9 1234                 # PID 1234 프로세스에 SIGKILL (강제 종료)
pkill -f "node app.js"       # 이름으로 프로세스 종료
killall nginx                # nginx 이름의 모든 프로세스 종료

# 백그라운드 실행
./script.sh &                # 백그라운드 실행
nohup ./script.sh &          # 터미널 종료 후에도 실행 유지
jobs                         # 백그라운드 작업 목록
fg %1                        # 작업 1을 포그라운드로
bg %1                        # 작업 1을 백그라운드에서 계속 실행
Ctrl+Z                       # 현재 작업 정지


# ==================== 네트워크 ====================

# 포트 확인
netstat -tulpn               # 열린 포트 확인 (TCP, UDP, 리스닝, PID)
ss -tulpn                    # netstat 대체 (더 빠름)
lsof -i :8080                # 8080 포트 사용 프로세스
lsof -i -P -n                # 모든 네트워크 연결

# 네트워크 테스트
ping google.com              # 연결 테스트 (Ctrl+C로 종료)
ping -c 4 google.com         # 4번만 테스트
curl https://api.example.com # HTTP 요청
curl -I https://example.com  # 헤더만 보기
curl -X POST -d '{"key":"value"}' -H "Content-Type: application/json" URL
wget https://example.com/file.zip  # 파일 다운로드
wget -O custom-name.zip URL        # 다른 이름으로 저장

# DNS 확인
nslookup example.com         # DNS 조회
dig example.com              # 상세 DNS 정보
host example.com             # 간단 DNS 조회


# ==================== 시스템 정보 ====================

# 디스크 사용량
df -h                        # 파일시스템 사용량 (human readable)
du -sh *                     # 현재 디렉토리 항목별 크기
du -sh /var/log              # 특정 디렉토리 크기
du -sh /var/log/* | sort -h  # 크기순 정렬

# 메모리 확인
free -h                      # 메모리 사용량
cat /proc/meminfo            # 상세 메모리 정보

# CPU 확인
cat /proc/cpuinfo            # CPU 정보
nproc                        # CPU 코어 수

# 시스템 정보
uname -a                     # 시스템 정보 (커널 버전 등)
uptime                       # 가동 시간 + 로드 평균
whoami                       # 현재 사용자
hostname                     # 호스트명
date                         # 현재 날짜/시간

# 디스크 I/O 모니터링
iostat                       # I/O 통계
iotop                        # I/O 실시간 모니터 (설치 필요)
```

### 예시 2: 서버 로그 분석하기 (상세)

```bash
# ==================== 실시간 로그 모니터링 ====================

# Nginx 액세스 로그 실시간 확인
tail -f /var/log/nginx/access.log

# 특정 패턴만 필터링하며 실시간 확인
tail -f /var/log/app/application.log | grep "ERROR"
tail -f /var/log/app/application.log | grep --line-buffered "ERROR"  # 버퍼링 없이

# 여러 로그 파일 동시에 보기
tail -f /var/log/nginx/*.log
multitail /var/log/nginx/access.log /var/log/nginx/error.log  # (설치 필요)

# 특정 시간대만 보기 (로그 형식에 따라 조정)
tail -f /var/log/app.log | grep "$(date +%Y-%m-%d)"


# ==================== 로그 분석 패턴 ====================

# 1. 에러 로그 분석
# 최근 500개 에러 로그
tail -500 /var/log/app/application.log | grep "ERROR"

# 에러 타입별 카운트
grep "ERROR" /var/log/app/application.log | \
  awk '{print $NF}' | sort | uniq -c | sort -rn

# 특정 시간대 에러
grep "2024-01-15 14:" /var/log/app/application.log | grep "ERROR"


# 2. Nginx 액세스 로그 분석 (Combined Log Format 기준)
# 로그 형식: IP - - [시간] "요청" 상태코드 바이트 "리퍼러" "UA"

# 상태 코드별 요청 수
awk '{print $9}' /var/log/nginx/access.log | sort | uniq -c | sort -rn
# 출력 예시:
#   15234 200
#    1023 404
#     512 500

# 500 에러 요청 상세 보기
awk '$9 == 500' /var/log/nginx/access.log

# IP별 요청 수 (DDoS, 비정상 트래픽 감지)
awk '{print $1}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head -20

# 요청 URL별 카운트
awk '{print $7}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head -20

# 특정 API 엔드포인트만
grep "POST /api/users" /var/log/nginx/access.log

# 응답 시간 분석 (로그에 응답 시간이 포함된 경우)
# $NF는 마지막 필드
awk '{print $NF}' /var/log/nginx/access.log | sort -n | tail -20

# 시간대별 요청 수
awk '{print $4}' /var/log/nginx/access.log | \
  cut -d: -f2 | sort | uniq -c

# 특정 사용자 에이전트 (모바일 앱 버전별)
grep "MyApp/1.2.3" /var/log/nginx/access.log | wc -l


# 3. JSON 형식 로그 분석 (jq 사용)
# jq 설치: apt install jq

# JSON 로그에서 특정 필드 추출
cat /var/log/app/json.log | jq '.level'
cat /var/log/app/json.log | jq 'select(.level == "error")'
cat /var/log/app/json.log | jq 'select(.userId == "123")'


# ==================== 로그 압축/관리 ====================

# 오래된 로그 압축
gzip application.log.1              # 압축 (원본 삭제)
gzip -k application.log.1           # 압축 (원본 유지)
gunzip application.log.1.gz         # 압축 해제

# 압축된 로그 검색 (압축 해제 없이)
zgrep "ERROR" application.log.*.gz
zcat application.log.1.gz | grep "ERROR"

# 로그 파일 크기 확인
du -sh /var/log/*

# 오래된 로그 삭제 (30일 이상)
find /var/log/app -name "*.log" -mtime +30 -delete

# 로그 로테이션 상태 확인
cat /etc/logrotate.d/nginx
```

### 예시 3: SSH로 서버 접속하기 (상세)

```bash
# ==================== SSH 기본 ====================

# 서버 접속
ssh ubuntu@192.168.1.100                           # 비밀번호 인증
ssh -i ~/.ssh/my-key.pem ubuntu@192.168.1.100      # 키 파일 인증
ssh -p 2222 ubuntu@192.168.1.100                   # 다른 포트

# SSH 키 생성 (한 번만 하면 됨)
ssh-keygen -t ed25519 -C "your-email@example.com"
# 결과:
#   ~/.ssh/id_ed25519     (개인키 - 절대 공유 X)
#   ~/.ssh/id_ed25519.pub (공개키 - 서버에 등록)

# RSA 키 생성 (레거시 시스템용)
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# 공개키를 서버에 등록 (자동)
ssh-copy-id ubuntu@192.168.1.100

# 수동으로 공개키 등록
cat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys  # 서버에서 실행


# ==================== SSH 편의 설정 (강력 추천!) ====================

# ~/.ssh/config 파일 생성
cat << 'EOF' > ~/.ssh/config
# 프로덕션 서버
Host prod
    HostName 192.168.1.100
    User ubuntu
    IdentityFile ~/.ssh/prod-key.pem
    Port 22

# 개발 서버
Host dev
    HostName 192.168.1.200
    User developer
    IdentityFile ~/.ssh/dev-key.pem

# 스테이징 서버 (점프 호스트 경유)
Host staging
    HostName 10.0.0.5
    User ubuntu
    ProxyJump bastion

# Bastion (점프 호스트)
Host bastion
    HostName 52.xx.xx.xx
    User ec2-user
    IdentityFile ~/.ssh/aws-key.pem

# 공통 설정
Host *
    ServerAliveInterval 60     # 60초마다 킵얼라이브
    ServerAliveCountMax 3      # 3번 실패하면 끊김
    AddKeysToAgent yes         # ssh-agent에 키 추가
    IdentitiesOnly yes         # 지정된 키만 시도
EOF

chmod 600 ~/.ssh/config

# 이제 간단히 접속 가능!
ssh prod
ssh dev
ssh staging


# ==================== 파일 전송 ====================

# scp: 단순 파일 복사
scp local_file.txt ubuntu@server:/home/ubuntu/       # 로컬 → 서버
scp ubuntu@server:/var/log/app.log ./                # 서버 → 로컬
scp -r ./project ubuntu@server:/home/ubuntu/         # 디렉토리 전송
scp -P 2222 file.txt ubuntu@server:/home/ubuntu/     # 다른 포트

# SSH config 사용 시
scp file.txt prod:/home/ubuntu/
scp prod:/var/log/app.log ./

# rsync: 효율적 동기화 (변경분만 전송) ★ 추천
rsync -avz ./project/ ubuntu@server:/home/ubuntu/project/
# -a: archive 모드 (권한, 시간 유지)
# -v: verbose (상세 출력)
# -z: 압축 전송

rsync -avz --delete ./project/ ubuntu@server:/project/
# --delete: 서버에만 있는 파일 삭제 (주의!)

rsync -avz --exclude 'node_modules' --exclude '.git' ./project/ ubuntu@server:/project/
# 특정 디렉토리 제외


# ==================== 포트 포워딩 (터널링) ====================

# 로컬 포트 포워딩
# 로컬 8080 → 서버의 localhost:3000
ssh -L 8080:localhost:3000 ubuntu@server
# 이제 http://localhost:8080 으로 서버의 3000 포트 접근 가능

# 실제 활용 예: RDS 접속
# 로컬 5432 → 서버 경유 → RDS:5432
ssh -L 5432:my-rds.xxx.rds.amazonaws.com:5432 ubuntu@bastion

# 리모트 포트 포워딩
# 서버의 8080 → 로컬의 3000
ssh -R 8080:localhost:3000 ubuntu@server
# 서버에서 localhost:8080 접속하면 로컬 3000으로 연결

# 백그라운드로 터널 유지
ssh -fNL 8080:localhost:3000 ubuntu@server
# -f: 백그라운드
# -N: 명령 실행 안 함 (터널만)
```

### 예시 4: 서비스 관리 (systemd) - 상세

```bash
# ==================== 서비스 관리 기본 ====================

# 서비스 상태 확인
sudo systemctl status nginx
# 출력 예시:
# ● nginx.service - A high performance web server
#    Loaded: loaded (/lib/systemd/system/nginx.service; enabled)
#    Active: active (running) since Mon 2024-01-15 10:30:00 KST
#  Main PID: 1234 (nginx)
#     Tasks: 5 (limit: 4915)
#    Memory: 10.5M
#    CGroup: /system.slice/nginx.service
#            ├─1234 nginx: master process
#            └─1235 nginx: worker process

# 서비스 시작/중지/재시작
sudo systemctl start nginx
sudo systemctl stop nginx
sudo systemctl restart nginx      # 완전 재시작 (잠시 중단)
sudo systemctl reload nginx       # 설정만 리로드 (무중단) ★

# 부팅 시 자동 시작 설정
sudo systemctl enable nginx       # 자동 시작 활성화
sudo systemctl disable nginx      # 자동 시작 비활성화
sudo systemctl is-enabled nginx   # 상태 확인

# 모든 서비스 목록
systemctl list-units --type=service
systemctl list-units --type=service --state=running  # 실행 중인 것만
systemctl list-units --type=service --state=failed   # 실패한 것만


# ==================== 서비스 로그 확인 (journalctl) ====================

# 특정 서비스 로그
sudo journalctl -u nginx                          # nginx 전체 로그
sudo journalctl -u nginx -f                       # 실시간 로그 ★
sudo journalctl -u nginx --since "1 hour ago"     # 최근 1시간
sudo journalctl -u nginx --since "2024-01-15"     # 특정 날짜 이후
sudo journalctl -u nginx --since "10:00" --until "11:00"  # 시간 범위
sudo journalctl -u nginx -n 100                   # 최근 100줄
sudo journalctl -u nginx -p err                   # 에러만 (err, warning, info, debug)

# 모든 로그
sudo journalctl                                   # 전체 시스템 로그
sudo journalctl -b                                # 현재 부팅 이후
sudo journalctl -b -1                             # 이전 부팅
sudo journalctl --disk-usage                      # 로그 용량 확인


# ==================== 서비스 생성 (예: Node.js 앱) ====================

# 서비스 파일 생성
sudo cat << 'EOF' > /etc/systemd/system/my-api.service
[Unit]
Description=My API Server
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/my-api
ExecStart=/usr/bin/node /home/ubuntu/my-api/server.js
Restart=always
RestartSec=10
Environment=NODE_ENV=production
Environment=PORT=3000

# 로그 설정
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

# 서비스 활성화 및 시작
sudo systemctl daemon-reload    # 서비스 파일 변경 후 필수!
sudo systemctl enable my-api
sudo systemctl start my-api
sudo systemctl status my-api
```

### 예시 5: 모바일 개발자를 위한 실전 시나리오

```bash
# ==================== 시나리오 1: API 500 에러 디버깅 ====================

# 1. 서버 접속
ssh prod

# 2. API 서버 상태 확인
sudo systemctl status api-server
# → Active: active (running) 확인

# 3. 최근 에러 로그 확인
sudo tail -100 /var/log/api-server/error.log | grep -A 5 "ERROR"

# 4. 실시간 로그 모니터링 (다른 터미널에서 요청하면서)
sudo tail -f /var/log/api-server/app.log | grep "ERROR\|Exception"

# 5. 특정 요청 추적 (Request ID로)
grep "req-id-12345" /var/log/api-server/app.log

# 6. 메모리/CPU 확인
top -p $(pgrep -f api-server)


# ==================== 시나리오 2: 배포 확인 ====================

# 1. 현재 배포 버전 확인
cat /home/ubuntu/api-server/version.txt
# 또는
curl localhost:3000/health | jq .version

# 2. 배포 로그 확인
tail -50 /home/ubuntu/deploy.log

# 3. 프로세스 재시작 시간 확인
systemctl show api-server --property=ActiveEnterTimestamp

# 4. 최근 변경 파일 확인
find /home/ubuntu/api-server -mmin -10 -type f


# ==================== 시나리오 3: 서버 성능 문제 ====================

# 1. 전체 시스템 상태
htop  # 또는 top

# 2. 디스크 공간 확인
df -h
# /dev/sda1  100G  95G  5G  95%  /   ← 위험!

# 3. 큰 파일 찾기
sudo du -sh /var/log/* | sort -h | tail -10

# 4. 오래된 로그 정리 (필요시)
sudo find /var/log -name "*.log.*.gz" -mtime +30 -delete

# 5. 메모리 사용량 확인
free -h
cat /proc/meminfo | grep -E "MemTotal|MemFree|MemAvailable"

# 6. 네트워크 연결 상태
ss -tulpn | grep LISTEN
netstat -an | grep ESTABLISHED | wc -l


# ==================== 시나리오 4: 빠른 디버깅 원라이너 ====================

# 최근 1시간 에러 수
grep "$(date -d '1 hour ago' '+%Y-%m-%d %H')" /var/log/app.log | grep -c ERROR

# IP별 요청 수 Top 10
awk '{print $1}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head -10

# 응답 시간 느린 요청 Top 10
awk '{print $NF, $7}' /var/log/nginx/access.log | sort -rn | head -10

# 500 에러 발생 URL
awk '$9 == 500 {print $7}' /var/log/nginx/access.log | sort | uniq -c | sort -rn

# 현재 연결 중인 클라이언트 수
ss -tn state established | wc -l
```

## 5. 장단점

### Linux의 장점

| 장점 | 설명 |
|------|------|
| 안정성 | 서버 수년간 재부팅 없이 운영 가능 |
| 보안성 | 권한 시스템, 빠른 보안 패치 |
| 무료/오픈소스 | 라이선스 비용 없음, 커스터마이징 가능 |
| 자원 효율성 | GUI 없이 운영하면 메모리/CPU 절약 |
| 자동화 친화적 | 스크립트로 모든 작업 자동화 가능 |
| 커뮤니티 | 방대한 문서와 지원 |

### 모바일 개발자 관점에서의 주의사항

| 주의사항 | 설명 |
|----------|------|
| 학습 곡선 | GUI 없이 명령어만 사용하는 것이 처음엔 어려움 |
| 권한 실수 | `rm -rf /`와 같은 치명적 실수 가능 |
| 환경 차이 | 로컬(Mac/Windows)과 서버(Linux) 차이 이해 필요 |
| 버전 차이 | Ubuntu, CentOS, Amazon Linux 등 배포판마다 차이 |

### 자주 하는 실수와 방지법

| 실수 | 방지법 |
|------|--------|
| `rm -rf /` 실행 | alias로 `rm -i` 설정, 삭제 전 `ls`로 확인 |
| 잘못된 권한 설정 | `chmod 777` 남용 금지, 최소 권한 원칙 |
| root로 모든 작업 | 일반 사용자로 작업, 필요시만 sudo |
| 로그 파일 가득 참 | logrotate 설정, 디스크 모니터링 |
| SSH 키 분실 | 키 백업, 여러 접근 방법 확보 |

## 6. 실무에서 자주 겪는 문제와 해결책

### 문제 1: Permission denied

```bash
# 원인 1: 파일 권한 없음
ls -la file.txt           # 권한 확인
sudo chmod 644 file.txt   # 읽기 권한 부여

# 원인 2: sudo 필요
sudo cat /var/log/secure

# 원인 3: SSH 키 권한
chmod 600 ~/.ssh/id_rsa
chmod 700 ~/.ssh
```

### 문제 2: 디스크 공간 부족

```bash
# 1. 용량 확인
df -h

# 2. 큰 디렉토리 찾기
sudo du -sh /* 2>/dev/null | sort -h

# 3. 로그 정리
sudo journalctl --vacuum-time=7d
sudo rm -f /var/log/*.gz

# 4. 캐시 정리
sudo apt clean
```

## 7. 내 생각

```
(이 공간은 학습 후 자신의 생각을 정리하는 곳입니다)

- Linux 명령어를 배우면서 새롭게 알게 된 점:


- 모바일 개발에서 서버 로그를 확인해야 했던 경험:


- 앞으로 자주 사용할 것 같은 명령어 Top 5:


```

## 8. 추가 질문

1. **Shell Script를 작성해서 반복 작업을 자동화하려면 어떻게 해야 하나요?** 배포 스크립트 예시를 만들어볼 수 있나요?

> **답변**: Shell Script는 리눅스 명령어들을 파일에 저장하여 순차적으로 실행하는 프로그램입니다. 파일 첫 줄에 `#!/bin/bash`를 추가하고, 실행 권한을 부여하면(`chmod +x script.sh`) 실행할 수 있습니다.
>
> 배포 스크립트 예시:
> ```bash
> #!/bin/bash
> set -e  # 에러 발생 시 중단
>
> APP_DIR="/home/ubuntu/my-app"
> BACKUP_DIR="/home/ubuntu/backups"
>
> echo "1. 현재 버전 백업..."
> cp -r $APP_DIR $BACKUP_DIR/backup-$(date +%Y%m%d-%H%M%S)
>
> echo "2. 코드 업데이트..."
> cd $APP_DIR
> git pull origin main
>
> echo "3. 의존성 설치..."
> npm install --production
>
> echo "4. 서비스 재시작..."
> sudo systemctl restart my-app
>
> echo "5. 상태 확인..."
> sleep 5
> curl -s http://localhost:3000/health
>
> echo "배포 완료!"
> ```
>
> 주요 팁: (1) `set -e`로 에러 시 자동 중단. (2) 변수 사용으로 경로 관리. (3) echo로 진행 상황 표시. (4) 백업 후 배포. (5) 완료 후 헬스체크. GitHub Actions나 Jenkins에서도 비슷한 스크립트를 YAML/Groovy로 작성합니다.

2. **Linux에서 환경변수는 어떻게 설정하고 관리하나요?** `.bashrc`, `.bash_profile`, `.env` 파일의 차이점은?

> **답변**: 환경변수는 프로세스가 참조할 수 있는 시스템 설정 값입니다. 설정 방법에 따라 적용 범위가 다릅니다.
>
> 파일별 차이점:
> - `~/.bashrc`: 새 터미널 창을 열 때마다 실행. alias, 함수, 환경변수 설정에 사용.
> - `~/.bash_profile` 또는 `~/.profile`: 로그인 시 한 번만 실행. PATH 설정 등에 사용.
> - `/etc/environment`: 시스템 전체에 적용되는 환경변수.
> - `.env`: 애플리케이션별 설정 파일 (dotenv 라이브러리가 로드).
>
> 설정 예시:
> ```bash
> # 임시 설정 (현재 세션만)
> export MY_VAR="value"
>
> # 영구 설정 (~/.bashrc에 추가)
> echo 'export MY_VAR="value"' >> ~/.bashrc
> source ~/.bashrc  # 즉시 적용
>
> # 서비스에 환경변수 전달 (systemd)
> # /etc/systemd/system/my-app.service
> Environment=NODE_ENV=production
> EnvironmentFile=/home/ubuntu/.env
> ```
>
> 모바일 개발자 관점: Xcode의 Scheme 환경변수, Android의 BuildConfig와 비슷한 개념입니다.

3. **서버 성능 모니터링은 어떻게 하나요?** CPU, 메모리, 디스크, 네트워크 사용량을 확인하는 방법은?

> **답변**: 실시간 모니터링 명령어:
> - `top` / `htop`: CPU, 메모리, 프로세스별 사용량 (htop이 더 직관적)
> - `free -h`: 메모리 사용량
> - `df -h`: 디스크 공간
> - `iostat`: 디스크 I/O
> - `iftop` / `nethogs`: 네트워크 사용량 (프로세스별)
> - `vmstat 1`: 가상 메모리 통계 (1초 간격)
>
> 지표 이해:
> - Load Average (uptime): CPU 부하. 코어 수보다 높으면 과부하
> - Memory의 "available": 실제 사용 가능한 메모리 (cache/buffer 포함)
> - %iowait (top): 디스크 I/O 대기 시간. 높으면 디스크 병목
>
> 프로덕션에서는 Prometheus + Grafana, DataDog, AWS CloudWatch 같은 모니터링 도구를 사용하여 알림을 설정합니다. 모바일 앱의 Crashlytics, Firebase Performance와 비슷한 역할입니다.

4. **crontab으로 작업을 스케줄링하려면 어떻게 해야 하나요?** 주기적인 로그 정리나 백업 설정 예시는?

> **답변**: crontab은 리눅스의 작업 스케줄러로, 정해진 시간에 명령을 자동 실행합니다.
>
> cron 표현식 형식: `분 시 일 월 요일 명령어`
> ```
> * * * * * command
> │ │ │ │ │
> │ │ │ │ └─ 요일 (0-7, 0과 7은 일요일)
> │ │ │ └─── 월 (1-12)
> │ │ └───── 일 (1-31)
> │ └─────── 시 (0-23)
> └───────── 분 (0-59)
> ```
>
> 예시:
> ```bash
> # 설정 열기
> crontab -e
>
> # 매일 새벽 3시에 로그 정리
> 0 3 * * * find /var/log/app -name "*.log" -mtime +7 -delete
>
> # 매주 일요일 2시에 백업
> 0 2 * * 0 /home/ubuntu/scripts/backup.sh >> /var/log/backup.log 2>&1
>
> # 5분마다 헬스체크
> */5 * * * * curl -s http://localhost:3000/health || systemctl restart my-app
>
> # 설정 확인
> crontab -l
> ```
>
> 주의사항: (1) 환경변수가 다르므로 절대 경로 사용. (2) 출력을 로그로 리다이렉트. (3) 실행 권한 확인.

5. **Linux 보안의 기본은 무엇인가요?** 방화벽(ufw/iptables), fail2ban, SSH 키 관리 등

> **답변**: 서버 보안 체크리스트:
>
> 1. **SSH 보안**:
>    - 비밀번호 인증 비활성화, 키 인증만 사용
>    - root 직접 로그인 비활성화 (`PermitRootLogin no`)
>    - 포트 변경 (22 → 다른 포트)
>
> 2. **방화벽 (ufw)**:
>    ```bash
>    sudo ufw allow 22/tcp      # SSH
>    sudo ufw allow 80/tcp      # HTTP
>    sudo ufw allow 443/tcp     # HTTPS
>    sudo ufw enable
>    ```
>
> 3. **fail2ban**: 무차별 대입 공격 방어
>    ```bash
>    sudo apt install fail2ban
>    # SSH 로그인 5번 실패 시 10분 차단 (기본 설정)
>    ```
>
> 4. **최소 권한 원칙**:
>    - 불필요한 서비스 비활성화
>    - 사용자별 최소 권한 부여
>    - root 사용 최소화, sudo 사용
>
> 5. **업데이트**:
>    ```bash
>    sudo apt update && sudo apt upgrade -y
>    # 또는 unattended-upgrades 설정
>    ```
>
> 모바일 개발자 관점: 앱의 Keychain/Keystore 사용, 권한 최소 요청, 인증서 피닝과 같은 보안 원칙이 서버에도 적용됩니다.

6. **AWS EC2나 GCP Compute Engine에서 Linux 서버를 운영할 때 알아야 할 것들은?** 클라우드 환경에서의 Linux 사용법

> **답변**: 클라우드 특화 지식:
>
> 1. **접속 방법**:
>    - AWS: `ssh -i key.pem ec2-user@IP` (Amazon Linux) 또는 `ubuntu@IP` (Ubuntu)
>    - GCP: `gcloud compute ssh instance-name`
>    - Session Manager (AWS): 웹 콘솔에서 SSH 없이 접속
>
> 2. **보안 그룹/방화벽**:
>    - AWS Security Group / GCP Firewall Rules
>    - 서버의 ufw/iptables보다 먼저 적용됨
>    - 필요한 포트만 열기 (22, 80, 443 등)
>
> 3. **메타데이터**:
>    ```bash
>    # AWS: 인스턴스 정보 조회
>    curl http://169.254.169.254/latest/meta-data/
>
>    # IAM Role 자격 증명 자동 획득
>    aws s3 ls  # 별도 키 설정 없이 실행 가능
>    ```
>
> 4. **로그 수집**:
>    - AWS CloudWatch Logs Agent
>    - 중앙 집중식 로그 관리
>
> 5. **스케일링 대응**:
>    - Auto Scaling Group으로 인스턴스 자동 증가/감소
>    - 상태 비저장(Stateless) 설계 중요
>    - 파일 저장은 S3/GCS, DB는 RDS/Cloud SQL 사용
>
> 모바일 개발자가 알아야 할 것: Firebase처럼 관리형 서비스와 달리, EC2는 직접 서버를 관리해야 합니다. 하지만 더 많은 제어권과 유연성을 제공합니다.
